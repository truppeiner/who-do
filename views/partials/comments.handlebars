<div class="comments my-2 rounded p-2">
  <p class="comment-title">Comments:</p>
  {{#each this}}
    <ul class="comment-list row mx-1">
      <li class="comment row">
        <p class="comment-user  text-start" >{{user.username}}:</p>
        <p class="comment-content  text-center" >{{comment_text}}</p>
      </li>
    </ul>
  {{/each}}
  </div>
    